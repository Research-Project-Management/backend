generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum EntityType {
  task
  paper
  page
  cycle
  file
  sticky
  comment
  collection
  worklog
  project
  work_item
  item
}
