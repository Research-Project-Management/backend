generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum EntityType {
  paper
  page
  cycle
  file
  sticky
  comment
  collection
  project
  work_item
}
